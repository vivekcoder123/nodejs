<h2 class="text-center my-4">Edit Subcategory</h2>

<form class="form" action="/admin/subcategories/edit/{{subcategory._id}}?_method=PUT" method="post" enctype="multipart/form-data">
  <div class="form-group row">
    <div class="col-md-6">
        <label for="name">Name</label>
	    <input type="text" name="name" value="{{subcategory.name}}" placeholder="subcategory name" class="form-control" required/>
    </div>
    <div class="col-md-6">
        <label for="category">Category</label>
        <select name="category" id="category" class="form-control">
            {{#select subcategory.category}}
            {{#each categories}}
                <option value="{{_id}}">{{name}}</option>
            {{/each}}
            {{/select}}
        </select>
    </div>
  </div>
  <div class="form-group mt-2">
    <label for="image">Image</label>
    <input type="file" name="image" placeholder="subcategory image" class="form-control"/>
  </div>
  <button type="submit" class="btn btn-info">Update Subcategory</button>
</form>